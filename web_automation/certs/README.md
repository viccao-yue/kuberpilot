# Certificates

Place administrator-approved private CA certificates in this directory.
Real certificates are ignored by Git. Do not use `verify=False` as a permanent workaround.

`mock-private-ca.crt` is generated only for the local integration fixture.
